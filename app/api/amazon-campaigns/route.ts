import { NextRequest, NextResponse } from 'next/server';
import { buildAmazonPlan } from '../../../lib/marketing/plans';
import { getSupabase } from '../../../lib/marketing/supabase';
export const runtime='nodejs';
export async function POST(req:NextRequest){try{const body=await req.json();const sku=String(body?.sku||'');const db=getSupabase();if(!db)return NextResponse.json({error:'Supabase não configurado.'},{status:503});const {data:profile}=await db.from('product_marketing_profiles').select('id').eq('sku',sku).maybeSingle();const {data:listing}=await db.from('prelistings').select('*').eq('sku',sku).maybeSingle();if(!listing||!profile)return NextResponse.json({error:'Perfil ou SKU não encontrado.'},{status:404});const product={...(listing.payload||{}),sku,title:listing.title};const plan=buildAmazonPlan(product,body.costs);const {data,error}=await db.from('amazon_campaign_plans').upsert({marketing_profile_id:profile.id,sku,status:'draft',plan,updated_at:new Date().toISOString()},{onConflict:'marketing_profile_id'}).select('*').single();if(error)throw error;return NextResponse.json({data,plan});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Falha ao criar plano Amazon.'},{status:500});}}
export async function GET(req:NextRequest){const db=getSupabase();const sku=req.nextUrl.searchParams.get('sku');if(!db)return NextResponse.json({error:'Supabase não configurado.'},{status:503});const query=db.from('amazon_campaign_plans').select('*');const {data,error}=sku?await query.eq('sku',sku).maybeSingle():await query.order('updated_at',{ascending:false});if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({data});}
