'use client';
import Link from 'next/link';
import { useEffect, useState } from 'react';
export default function MarketingPage(){const [rows,setRows]=useState<any[]>([]);const [error,setError]=useState('');useEffect(()=>{fetch('/api/marketing-profiles').then(r=>r.json()).then(d=>{if(!d.data&&d.error)setError(d.error);setRows(d.data||[])}).catch(e=>setError(e.message))},[]);return <div className="app-shell"><aside className="sidebar"><div className="mb-8 text-xl font-black text-white">FBRSigns · Marketing</div><Link href="/dashboard" className="block px-3 py-2.5 text-sm text-slate-400">← Catálogo</Link><div className="rounded-lg bg-slate-800 px-3 py-2.5 text-sm text-white">▣ Marketing readiness</div></aside><main className="main"><header className="mb-7"><div className="text-[11px] font-extrabold uppercase tracking-[.14em] text-blue-600">MP-001 · planos revisáveis</div><h1 className="mb-1 mt-1 text-3xl font-black">Marketing readiness</h1><p className="text-slate-500">Margem real, Amazon PPC, Meta Ads, tracking e launch gate por SKU</p></header><div className="card">{error&&<div className="mb-4 text-sm text-amber-700">{error} Configure o Supabase para carregar dados.</div>}{rows.length===0?<div className="py-10 text-center text-sm text-slate-500">Nenhum perfil encontrado. Salve um PreListing e crie o perfil pela API.</div>:<div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-4 py-3">SKU</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Margem</th><th className="px-4 py-3">Meta BM</th><th className="px-4 py-3">Ação</th></tr></thead><tbody className="divide-y">{rows.map(r=><tr key={r.sku}><td className="px-4 py-3 font-bold">{r.sku}</td><td className="px-4 py-3">{r.status}</td><td className="px-4 py-3">{r.economics?.margin?.percentage != null ? `${r.economics.margin.percentage}%` : 'bloqueada'}</td><td className="px-4 py-3">{r.audience?.business_manager_id || '—'}</td><td className="px-4 py-3"><Link className="text-blue-600 hover:underline" href={`/marketing/${encodeURIComponent(r.sku)}`}>Abrir painel</Link></td></tr>)}</tbody></table></div>}</div></main></div>}
